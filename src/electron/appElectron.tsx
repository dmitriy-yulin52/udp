import * as Raect from 'react'






export const AppElectron = ():Raect.ReactElement=> {

    

    return <div>
        <button onClick={()=>{}}>saveFile</button>
        <button onClick={()=>{}}>writeFile</button>
        <button onClick={()=>{}}></button>
    </div>
}
